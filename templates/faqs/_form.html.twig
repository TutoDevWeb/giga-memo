{{ form_start(form) }}

{{ form_errors(form) }}

{# Champ Texte du Nom #}
{{ form_label(form.name, 'Nom de la FAQ', { 'label_attr': {'class': 'mt-4'} }) }}
{{ form_errors(form.name) }}
{{ form_widget(form.name) }}

{# Champ Category #}
{{ form_label(form.category, 'Catégorie', { 'label_attr': {'class': 'mt-4'} }) }}
{{ form_errors(form.category) }}
{{ form_widget(form.category) }}

{# Champ duré #}
{{ form_label(form.duration, 'Durée d\'exécution en minutes', { 'label_attr': {'class': 'mt-4'} }) }}
{{ form_widget(form.duration.minutes) }}

<button class="btn btn-primary mt-4">
	<i class="bi bi-{{ button_icon|default('check-circle') }} me-2"></i>
	{{ button_label|default('Default') }}
</button>

{{ form_end(form) }}
