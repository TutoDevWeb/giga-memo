<button type="button" class="btn btn-delete-trigger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteConfirmModal" data-action="{{ path('app_faqs_delete', {'id_faq': faq.id}) }}" data-token="{{ csrf_token('delete' ~ faq.id) }}">
	Supprimer la FAQ
</button>
{% include "_partials/_delete_modal.html.twig" %}
