<button type="button" class="btn btn-delete-trigger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteConfirmModal" data-action="{{ path('app_categories_delete', {'id': category.id}) }}" data-token="{{ csrf_token('delete' ~ category.id) }}">
	Supprimer
</button>
{% include "_partials/_delete_modal.html.twig" %}
