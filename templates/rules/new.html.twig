{% extends 'base.html.twig' %}

{% block title %}New Rules
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col">

				<h1>Create new Rules</h1>

				{{ include('rules/_form.html.twig') }}

				<a href="{{ path('app_rules_index',{ 'id_faq': faq.id }) }}">back to list</a>

			</div>
		</div>
	</div>

{% endblock %}
