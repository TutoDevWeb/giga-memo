{% extends 'base.html.twig' %}

{% block title %}Edit Rules
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col">
				<h1>Edit Rules</h1>

				{{ include('rules/_form.html.twig', {'button_label': 'Update'}) }}

				<a class='btn btn-primary mt-4' href="{{ path('app_rules_index',{'id_faq' : faq.id }) }}">back to list</a>

				{{ include('rules/_delete_form.html.twig') }}

			</div>
		</div>
	</div>
{% endblock %}
