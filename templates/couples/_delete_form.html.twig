<form method="post" action="{{ path('app_couples_delete', {'id_faq': faq.id,'id_couple': couple.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couple.id) }}">
	<button class="btn btn-primary">Supprimer le QR</button>
</form>
