<button type="button" class="btn btn-delete-trigger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteConfirmModal" data-action="{{ path('app_couples_delete', {'id_faq': faq.id, 'id_couple': couple.id}) }}" data-token="{{ csrf_token('delete' ~ couple.id) }}">
	Supprimer la QR
</button>
{% include "_partials/_delete_modal.html.twig" %}
